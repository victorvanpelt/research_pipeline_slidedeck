# Local AI-agent configuration; not part of the deck
.claude/
.mcp.json

# Zero-byte junk some local process keeps creating
node_modules/
package.json
package-lock.json
yarn.lock
pnpm-lock.yaml
bunfig.toml
.npmrc
.yarnrc*
.env
.env.*

# Self-study lesson package (local working material, not part of the deck)
ai_agents_package/
